# PC-side ingest service dependencies
pyserial>=3.5          # reading the ESP32 over USB (COM port)
psycopg2-binary>=2.9   # PostgreSQL / TimescaleDB backend
# sqlite3 is in the Python standard library (no install needed)

# Only needed for `python test_device_clock.py --pg`, which starts a throwaway
# PostgreSQL so the device-time path can be tested without touching your data.
# The tests without --pg need nothing beyond the standard library.
pgserver>=0.1          # optional
